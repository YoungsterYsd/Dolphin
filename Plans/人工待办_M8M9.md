# 人工待办清单（M8 + M9 开发期间）

> 主 agent 在 M8/M9 开发过程中遇到**无法用 MCP/脚本自动验证**或**需要美术资源/Git 操作/视觉品味决策**等场景时，会把待办项追加到本文件，请用户在合适的时机批量处理。
>
> 完成一项后，把 `[ ]` 改为 `[x]` 即可。

---

## 🔴 阻塞项（影响 M8/M9 推进）

> 当前无阻塞项。

---

## 🟡 可异步处理项（不阻塞但建议尽快做）

### M8.2 · SFX 音频资源补全
- [ ] **`Data/Config/SfxBindings.tres` 填充实际 AudioStream**
  - 当前位置：`d:/Demos/Godot/Dolphin/Data/Config/SfxBindings.tres`
  - 主 agent 已创建空表骨架，但 8 个 sfx_id 的实际 AudioStream 资源需要美术/音效师补：
    - `hit_normal` - 普通命中音效（短促"砰"）
    - `hit_critical` - 暴击命中音效（更厚重）
    - `parry` - 招架/格挡音效（金属碰撞声）
    - `weapon_swing` - 挥击破空音效
    - `enemy_hurt` - 敌人受击呻吟
    - `boss_phase_change` - Boss 切阶段提示音
    - `ui_click` - UI 按钮点击
    - `pickup_item` - 拾取道具
  - **临时方案**：可以从 `res://Content/Audio/` 现有 4 个 mp3 + 1 个 ogg 里随便挑映射先验证流程，资源后期再替换

### M8.3 · 字体资源
- [ ] **伤害飘字字体**
  - 已有 `Content/Uranus_Pixel_11Px.ttf` 像素字体，飘字若使用此字体需要在 DamagePopup.tscn 里手动指定（主 agent 默认使用系统字体）
  - 若需更换字体，编辑 `Scenes/UI/DamagePopup.tscn` 的 Label.font 属性

### M8.6 · VFX 占位资源
- [ ] **VFX 场景占位（M8 不强制，M9 必须）**
  - M8 期 `VFXSpawner` 只做路由订阅，没具体调用 → 不阻塞
  - M9 期至少需要 1-2 个 3D VFX 占位场景在 `Content/VFX/` 下，供技能命中时实例化：
    - `Content/VFX/Slash_3D.tscn` - 挥砍光效（GPUParticles3D + 简单飘带）
    - `Content/VFX/Impact_3D.tscn` - 命中爆点（短粒子爆发）
  - **临时方案**：M9 时主 agent 用纯 GPUParticles3D 简单参数生成，无贴图

---

## 🟢 验收前手动检查项（M8 完成时）

- [ ] **关闭并重启 Godot 编辑器**：使用 `Tools\godot.bat restart`（M8 大量新建组件，确保 GDScript 缓存刷新）
- [ ] **TestArena 内手动验证**：
  - 玩家攻击 Slime，看到敌人头顶血条扣血、敌人闪白、屏幕震动、伤害飘字
  - HitFeedbackConfig.tres 中改 `default_camera_shake_intensity=10`，再攻击应明显感到震动加强
  - HealthBarConfig.tres 中改 `overhead_show_distance=200`，远处敌人血条应隐藏

---

## 🔴 M9 启动前必做（Git 操作）

- [ ] **打 Git 标签 v0.5-pre-hd2d**
  - 进 Dolphin 工作区根目录，执行：
    ```bash
    git add -A
    git commit -m "M8 done, prep for HD-2D M9"
    git tag v0.5-pre-hd2d
    ```
  - 主 agent 不会自动执行 git 命令（避免污染历史），由你手动确认时机

- [ ] **规则文档 R-CHAR-02 激活确认**
  - 主 agent 在 M9 启动前会再一次 review 全局规则.md 里 R-CHAR-02 的描述是否还有疏漏
  - 你确认后即可开始 M9.1

---

## 🟢 验收前手动检查项（M9 完成时）

- [ ] **关闭并重启 Godot 编辑器**：使用 `Tools\godot.bat restart`
- [ ] **TestArena 3D 化后视觉确认**：
  - 镜头俯视角度看着舒服（不舒服可改 `CameraConfig.tres` 的 pitch/distance/fov）
  - 角色 Sprite3D billboard 朝镜头
  - DOF/Bloom/SSAO 开启时 fps ≥60
- [ ] **回归测试**：M3 普攻 / M4 战斗 / M5 拾取装备 / Boss 阶段切换 全部正常
- [ ] **HUD 显示**：血条 / 技能槽 / 背包 / 头顶血条 / 飘字 全部在 3D 场景下正常显示

---

## 📋 已知工程踩坑（参考用）

| 踩坑 | 解决方案 |
|---|---|
| GDScript 缓存导致 `@tool` 脚本改动不生效 | 用 `Tools\godot.bat restart` |
| Camera2D `make_current` 在 add_child 前调用报错 | M7.7 已修：依靠 SubViewport 自动选择当前相机 |
| MCP `execute_editor_script` 中 PowerShell 把 `$_` 吃掉 | 改用直接 cmd / tasklist |
| .bat 注释 `::` + 中文导致 cmd 误解析 | M8 期所有新 .bat 必须用 `rem` 注释 + 注释行避免中文 |

---

## 📦 主 agent 对人工待办的承诺

1. 任何**阻塞当前任务推进**的需求 → 立即写到 🔴 阻塞项 + 在对话中明确告知用户
2. **可继续推进但需要用户后续补**的资源 → 写到 🟡 可异步项 + 跑测试时用占位
3. **每个里程碑完成时**主动汇总该里程碑内累积的待办，在对话中给出"📋 本里程碑待办摘要"
4. **Git 操作**绝不擅自执行，全部列到待办由用户手动
