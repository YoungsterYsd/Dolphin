# 09 · 对话系统落地路线（Phase 0~3）

> **文档定位**：把 `08_对话系统设计.md` 拆成可交付的 commit 级 Todo，给出每一项的验收内容。
> **状态**：路线锁定；具体台词文案 / 肖像图 / 打字机速度挂起。
> **生成日期**：2026-05-21
> **建议节奏**：Phase 0+1+2 必须打包完成（架构地基不能塌）；Phase 3 可按战场需要切片。
> **总工期预估**：5~7 人日（与三期开发计划 M11 估算一致）。

---

## 0. 路线总览

| 阶段 | 名称 | 工期 | 必做 | 输出 |
|---|---|---|---|---|
| **Phase 0** | 前置基建 | 0.5d | ✅ | EventBus 5 信号 + DialogueConfig.tres + Dialogue.tres 强化 |
| **Phase 1** | 数据层 | 1d | ✅ | DialogueGraph + 3 NodeKind + NextLink + ChoiceOption + DialogueExpr |
| **Phase 2** | 运行时 | 1.5d | ✅ | DialogueRunner Autoload + EffectHandlerRegistry + 4 内置 Handler |
| **Phase 3** | UI 层 | 1.5d | ✅ | DialogueWidget extends BaseWidget + 接 HUD 栈 + 接 InputContext |
| **Phase 4** | 调试与示例 | 0.5d | ⭕ | F8/F9 Cheat + DebugOverlay 扩展 + 1 份示例 graph |
| **Phase 5** | Excel 联调 | 0.5d | ⭕ | 等 M10 工作流先成熟，再做 Excel → Dialogue.json |

> **里程碑约束**：Phase 0+1+2+3 共 ≈ 4.5d 必须打包完成；中间态没法跑通。

---

## Phase 0 · 前置基建（0.5d）

### 0.1 Todo 清单

| # | Todo | 类型 | 工期 | 交付 |
|---|---|---|---|---|
| **D0-T1** | EventBus 加 5 信号：dialogue_started / dialogue_node_changed / dialogue_choice_presented / dialogue_ended / dialogue_effect_dispatched | 改造 | 0.1d | EventBus.gd 加 5 signal |
| **D0-T2** | GameInstance 加 `dialogue_vars: Dictionary[StringName, Variant]` + `set_dialogue_var / get_dialogue_var` API | 改造 | 0.1d | GameInstance.gd 加 3 项 |
| **D0-T3** | 检查 `Data/Config/InputContexts/Dialogue.tres`：确认 allow_actions 仅含 `combat_interact / cancel`，blocked 包含战斗 action（已存在，仅审一遍）| 配置 | 0.1d | 审计报告 |
| **D0-T4** | 新建 `Data/Manual/Config/DialogueConfig.tres`：typewriter_speed=40 / auto_advance_seconds=-1 / default_portrait=空 | 配置 | 0.1d | 1 份 .tres |
| **D0-T5** | 新建 `Data/Manual/Config/PortraitsConfig.tres`：portrait_id → texture path Dictionary（默认空，先建文件） | 配置 | 0.1d | 1 份 .tres |

### 0.2 验收点

| ID | 验收点 | 测试方式 | 通过标准 |
|---|---|---|---|
| D0-A1 | EventBus 5 信号存在且类型正确 | 运行时 `EventBus.has_signal(&"dialogue_*")` | 5 个全部 true |
| D0-A2 | GameInstance.dialogue_vars 读写正常 | 运行时 `set_var("test", 1) + get_var("test")` | 返回 1 |
| D0-A3 | Dialogue.tres 屏蔽战斗 action | InputContextManager.push(Dialogue) 后调 is_action_allowed("combat_attack") | false |
| D0-A4 | DialogueConfig / PortraitsConfig 资源加载成功 | `load(".../DialogueConfig.tres")` 不返回 null | OK |

---

## Phase 1 · 数据层（1d）

### 1.1 Todo 清单

| # | Todo | 类型 | 工期 | 交付 |
|---|---|---|---|---|
| **D1-T1** | `Script/Dialogue/NextLink.gd`（Resource）：`next_id / condition_expr` | 新增 | 0.05d | 1 个 .gd |
| **D1-T2** | `Script/Dialogue/ChoiceOption.gd`（Resource）：`text / condition_expr / next_id` | 新增 | 0.05d | 1 个 .gd |
| **D1-T3** | `Script/Dialogue/DialogueNode.gd` 基类（Resource）：`node_id / next_links: Array[NextLink]` + 虚方法 `get_node_kind()` | 新增 | 0.1d | 1 个 .gd |
| **D1-T4** | `Script/Dialogue/Nodes/SpeechNode.gd`：`speaker / portrait_id / text / voice_id`，get_node_kind() 返回 &"speech" | 新增 | 0.05d | 1 个 .gd |
| **D1-T5** | `Script/Dialogue/Nodes/ChoiceNode.gd`：`prompt / choices: Array[ChoiceOption]`，get_node_kind() 返回 &"choice" | 新增 | 0.05d | 1 个 .gd |
| **D1-T6** | `Script/Dialogue/Nodes/EffectNode.gd`：`effect_kind / payload: Dictionary`，get_node_kind() 返回 &"effect" | 新增 | 0.05d | 1 个 .gd |
| **D1-T7** | `Script/Dialogue/DialogueGraph.gd`（Resource）：`graph_id / start_node_id / nodes: Array[DialogueNode]` + `get_node(id) -> DialogueNode` 查找方法 | 新增 | 0.1d | 1 个 .gd |
| **D1-T8** | `Script/Dialogue/DialogueExpr.gd`（静态工具）：`eval(expr: String, ctx: Dictionary) -> bool`，支持 HasTag / Var.X op N / && / \|\| / ! / 括号 | 新增 | 0.4d | 1 个 .gd + 内嵌单测 |
| **D1-T9** | `Script/Dialogue/DialogueGraphLoader.gd`：`load(graph_id) -> DialogueGraph`，先查 Generated/Dialogue.json，再 fallback `Data/Manual/Dialogues/*.tres` | 新增 | 0.15d | 1 个 .gd |
| **D1-T10** | ConfigCenter 加 `get_dialogue_graph(graph_id) -> DialogueGraph`，内部调 DialogueGraphLoader | 改造 | 0.05d | ConfigCenter.gd 加 1 API |

### 1.2 验收点

| ID | 验收点 | 测试方式 | 通过标准 |
|---|---|---|---|
| D1-A1 | 5 个 NodeKind 都能在编辑器中创建 .tres | 编辑器 Inspector 中右键 → New Resource → 找到 | 5 个全部可见 |
| D1-A2 | DialogueGraph 能持有 nodes 数组 | 手写 1 份 .tres 含 3 个 node | 加载后 nodes.size() == 3 |
| D1-A3 | DialogueExpr 8 用例全过 | 内嵌单测：`HasTag(...)` / `Var.X > N` / `&&` / `\|\|` / `!` / 括号 / 嵌套 / 空表达式 | 8 用例 PASS |
| D1-A4 | ConfigCenter.get_dialogue_graph 返回非空 | 加载 1 份示例 graph | 非空、graph_id 正确 |

---

## Phase 2 · 运行时（1.5d）

### 2.1 Todo 清单

| # | Todo | 类型 | 工期 | 交付 |
|---|---|---|---|---|
| **D2-T1** | `Script/Dialogue/EffectHandlerRegistry.gd`（Autoload）：register / unregister / dispatch(kind, payload, ctx) | 新增 | 0.2d | 1 Autoload + project.godot 注册 |
| **D2-T2** | `Script/Dialogue/Handlers/SetVarHandler.gd`：set_var payload → GameInstance.set_dialogue_var | 新增 | 0.05d | 1 个 .gd |
| **D2-T3** | `Script/Dialogue/Handlers/AddTagHandler.gd`：add_tag payload → 玩家 ASC.add_loose_tag | 新增 | 0.05d | 1 个 .gd |
| **D2-T4** | `Script/Dialogue/Handlers/PlaySfxHandler.gd`：play_sfx payload → AudioManager.play_sfx | 新增 | 0.05d | 1 个 .gd |
| **D2-T5** | `Script/Dialogue/Handlers/HudToastHandler.gd`：hud_toast payload → EventBus.hud_toast_requested.emit | 新增 | 0.05d | 1 个 .gd |
| **D2-T6** | `Script/Dialogue/DialogueRunner.gd`（Autoload）：start / advance / select_choice / end / force_end + 内部 _enter_node 三分支 + 文本插值 | 新增 | 0.6d | 1 Autoload + project.godot 注册 |
| **D2-T7** | DialogueRunner 启停联动 InputContext + HUDStateMachine（emit hud_state_changed）| 集成 | 0.2d | runner 内逻辑 |
| **D2-T8** | 内置 4 Handler 在 EffectHandlerRegistry._ready 内自动注册 | 集成 | 0.05d | registry _ready |
| **D2-T9** | 文本占位符解析（`{var:x}` / `{tag:x}` / `{player_name}`） | 新增 | 0.2d | runner 内 _resolve_text 方法 |
| **D2-T10** | 1 份纯代码示例 graph（运行时 new + 加节点）跑通：speech → choice → effect → speech → end | 测试 | 0.1d | 临时测试脚本 + 日志验证 |

### 2.2 验收点

| ID | 验收点 | 测试方式 | 通过标准 |
|---|---|---|---|
| D2-A1 | EffectHandlerRegistry 注册/查询正常 | register(&"set_var", h) + dispatch(&"set_var", {...}) | h.handle 被调用 |
| D2-A2 | DialogueRunner.start 后 EventBus 信号顺序正确 | 订阅信号记录顺序 | dialogue_started → dialogue_node_changed (speech) |
| D2-A3 | select_choice 走对分支 | 2 选项 graph，分别选 0/1 | 分别走到不同 next |
| D2-A4 | EffectNode 触发 + 自动 next | effect 节点后接 speech | effect_dispatched 后 node_changed 到 speech |
| D2-A5 | 条件 next_link 正确过滤 | 2 条 next，第 0 条 condition false | 第 1 条胜出 |
| D2-A6 | InputContext 联动 | start 后查 InputContextManager.get_current_id() | == &"Dialogue" |
| D2-A7 | HUDStateMachine 联动 | start 后查 HUDStateMachine.get_current_state() | == State.DIALOGUE |
| D2-A8 | force_end 干净退出 | start → force_end 立刻调 | InputContext / State 还原；未触发 Effect |
| D2-A9 | 文本插值生效 | `Hello, {var:player_name}!` + dialogue_vars.player_name=Alice | "Hello, Alice!" |
| D2-A10 | 重复 start 不堆叠 | start(A) + start(B) | warn + 第二次忽略，仍在 A |

---

## Phase 3 · UI 层（1.5d）

### 3.1 Todo 清单

| # | Todo | 类型 | 工期 | 交付 |
|---|---|---|---|---|
| **D3-T1** | `Scenes/UI/Widgets/DialogueWidget.tscn`：底部全宽 Panel + Portrait + Speaker Label + RichTextLabel + ChoicesVBox + ContinueIndicator | 新增 | 0.3d | 1 个 .tscn |
| **D3-T2** | `Script/UI/Widgets/DialogueWidget.gd` extends BaseWidget：订阅 EventBus.dialogue_node_changed / dialogue_choice_presented / dialogue_ended；按 NodeKind 渲染 | 新增 | 0.4d | 1 个 .gd |
| **D3-T3** | 打字机效果（RichTextLabel.visible_characters tween，速度走 DialogueConfig.typewriter_speed） | 新增 | 0.2d | DialogueWidget 内方法 |
| **D3-T4** | 输入处理：speech 按 combat_interact 推进；choice ↑↓ 切换 + combat_interact 确认（订阅 EventBus.player_input_action_pressed） | 新增 | 0.2d | DialogueWidget 内方法 |
| **D3-T5** | DialogueRunner.start 内：实例化 DialogueWidget → HUDManager.push_widget(&"L2_GameMenu", w)；end 内 pop_widget | 集成 | 0.15d | DialogueRunner 内逻辑 |
| **D3-T6** | Choice 按钮池（避免每次 instantiate）：最多 6 个，复用 | 优化 | 0.1d | DialogueWidget 内池 |
| **D3-T7** | Portrait 加载（用 PortraitsConfig.portrait_paths 表查 portrait_id → texture） | 集成 | 0.1d | DialogueWidget._update_portrait |
| **D3-T8** | 入场 / 退场 tween（淡入 + 上滑；时长走 UIDurations.S） | 优化 | 0.05d | DialogueWidget 内 |

### 3.2 验收点

| ID | 验收点 | 测试方式 | 通过标准 |
|---|---|---|---|
| D3-A1 | DialogueWidget 通过 HUDManager.push_widget 入栈（首次验证 P1-A4！） | runner.start 后查 HUDManager.get_stack_size(&"L2_GameMenu") | == 1 |
| D3-A2 | 战斗 action 被屏蔽 | 对话中按 A | 不触发普攻（HUD log 无 combat_attack） |
| D3-A3 | 打字机效果可见 | speech 文本 60 字符 | 1.5s 内逐字显示完 |
| D3-A4 | 任意键跳过打字 | 打字中按空格 | 立刻全显 |
| D3-A5 | combat_interact 推进 | speech 完成态按 E | 进入下一节点 |
| D3-A6 | 选项 ↑↓ 切换 + E 确认 | choice 节点按 ↓↓Enter | 选中第三项 |
| D3-A7 | 不满足条件的选项不显示 | 第 2 选项 condition=false | 仅显示其他选项 |
| D3-A8 | 对话结束后 HUD 恢复 | end 后查栈深度 / InputContext / HUDState | 全部还原 |
| D3-A9 | Portrait 缺失图时显示占位 | portrait_id 不在表中 | 显示 default_portrait |

---

## Phase 4 · 调试与示例（0.5d · 可切片）

### 4.1 Todo 清单

| # | Todo | 类型 | 工期 | 交付 |
|---|---|---|---|---|
| **D4-T1** | HUDDebugCheats 加 F8（启动 `debug_test_graph`）+ F9（force_end） | 改造 | 0.05d | HUDDebugCheats.gd |
| **D4-T2** | `Data/Manual/Dialogues/debug_test_graph.tres`：含 speech / choice / effect 三种节点 + 一个 set_var 后再 speech 引用变量 | 配置 | 0.2d | 1 个 .tres |
| **D4-T3** | `Data/Manual/Dialogues/example_blacksmith_intro.tres`：5 节点完整剧情（含分支 + give_item Effect 占位）| 配置 | 0.15d | 1 个 .tres |
| **D4-T4** | DebugOverlayWidget 加一行 `Dialogue: <graph_id> / <node_id>` | 改造 | 0.05d | DebugOverlay.gd |
| **D4-T5** | DialogueRunner 加 `get_current_graph_id() / get_current_node_id()` API（debug 用） | 改造 | 0.03d | runner.gd |
| **D4-T6** | 全流程验收（按 F8 → 完整走完，按 F11 看 DebugOverlay 正确）| 测试 | 0.02d | 验收报告 |

### 4.2 验收点

| ID | 验收点 | 测试方式 | 通过标准 |
|---|---|---|---|
| D4-A1 | F8 启动测试对话 | 游戏中按 F8 | DialogueWidget 出现 |
| D4-A2 | F9 强制结束 | F8 后立刻 F9 | DialogueWidget 消失 |
| D4-A3 | DebugOverlay 显示对话位置 | 对话中按 F11 | 看到 `Dialogue: debug_test_graph / node_2` |
| D4-A4 | 示例对话 set_var → 后续 speech 插值正确 | 走完 example_blacksmith_intro | 文本中变量被替换 |
| D4-A5 | 总耗时 ≤ 当时帧时间预算 | 全程查 FPS | ≥ 55 fps |

---

## Phase 5 · Excel 联调（0.5d · 与 M10 并行）

> 仅当 M10 通用 emitter 已就位时启动。M11 阶段先用 .tres 手写。

### 5.1 Todo 清单

| # | Todo | 类型 | 工期 | 交付 |
|---|---|---|---|---|
| **D5-T1** | Excel `Dialogue` sheet 模板（按 M11 草案 §M11.1 表头）| 配置 | 0.1d | 1 份 Excel + 示例数据 |
| **D5-T2** | DialogueGraphLoader 增加从 `Generated/Dialogue.json` 加载分支 | 改造 | 0.2d | loader.gd |
| **D5-T3** | M10 通用 emitter 跑通 Dialogue sheet → Generated/Dialogue.json | 测试 | 0.15d | 工具运行 + 检查输出 |
| **D5-T4** | 示例对话从 .tres 迁移到 Excel 行 | 改造 | 0.05d | Excel + 删除旧 .tres |

### 5.2 验收点

| ID | 验收点 | 测试方式 | 通过标准 |
|---|---|---|---|
| D5-A1 | Excel 行成功导出 Dialogue.json | 运行 emitter | 文件存在 + 合法 JSON |
| D5-A2 | DialogueGraphLoader 优先 Json | 同 graph_id 在 .tres 和 .json 都存在 | 用 .json 版本 |
| D5-A3 | 示例对话从 Excel 跑通 | F8 启动 | 行为与从 .tres 加载一致 |

---

## 累计统计

| 阶段 | Commit Todo | 验收点 | 工期 |
|---|---|---|---|
| Phase 0 | 5 | 4 | 0.5d |
| Phase 1 | 10 | 4 | 1.0d |
| Phase 2 | 10 | 10 | 1.5d |
| Phase 3 | 8 | 9 | 1.5d |
| Phase 4 | 6 | 5 | 0.5d |
| Phase 5 | 4 | 3 | 0.5d |
| **合计** | **43** | **35** | **5.5d** |

---

## 关键里程碑节点

| Day | 节点 |
|---|---|
| **Day 0.5** | Phase 0 完成；EventBus 5 信号 + Config 资源就位 |
| **Day 1.5** | Phase 1 完成；可以手写 .tres 对话图（无法运行） |
| **Day 3.0** | Phase 2 完成；纯代码可跑通对话流程（无 UI） |
| **Day 4.5** | Phase 3 完成；**真实业务可玩，DialogueWidget 全功能** |
| **Day 5.0** | Phase 4 完成；Cheat + 示例 + DebugOverlay 接好，**M11 完成基线** |
| **Day 5.5** | Phase 5 完成；Excel 工作流接入 |

---

## 与 HUD 系统前置依赖的兑现关系

| HUD DEP | 兑现节点 | 兑现方式 |
|---|---|---|
| **DEP-11** InputContext.Dialogue 实战切换 | Phase 2-T7 | DialogueRunner.start/end 自动 push/pop |
| **DEP-12** HUDStateMachine 进入 DIALOGUE | Phase 2-T7 | DialogueRunner emit hud_state_changed |
| **DEP-13** HUDManager push/pop 栈管理（首次验证 P1-A4）| Phase 3-T5 | DialogueWidget 走 push_widget(L2_GameMenu) |

**对话系统是 HUD 前置依赖 DEP-11/12/13 的天然兑现者**。完成本路线即关闭 HUD 系统 3 个最关键的前置依赖。

---

## 变更日志

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1 | 2026-05-21 | 首次建立。43 commit + 35 验收点 + 5.5d 工期；与 HUD 系统前置依赖对接清晰。|
