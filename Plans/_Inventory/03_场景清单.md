# 03 · 场景清单（C0-T3）

> **生成日期**：2026-05-21
> **统计**：
> - `Scenes/**/*.tscn`：12 份
> - `addons/skill_editor/**/*.tscn`：2 份
> - `addons/godot_mcp/**/*.tscn`：1 份（插件内置 UI）
> - **合计：15 份场景**

---

## 一、Scenes/Main/（1 份）

| 文件 | 根节点 | 挂载脚本 | 关键 ExtResource | 角色 |
|---|---|---|---|---|
| `Main/main_scene.tscn` | Node3D | `Scenes/Main/main_scene.gd` | （TrainingDummy 内嵌 SubResource(DummyAttrSet)） | 入口主场景；project.godot run/main_scene 早期值（已切到 TestArena）|

> ✅ `main_scene.tscn` 对 `DefaultPlayerAttributes.tres` 的依赖已于 2026-05-21 解除（DIR-2），TrainingDummy 改用 inline SubResource。

## 二、Scenes/Characters/（3 份）

| 文件 | 根节点 | 挂载脚本 | 关键资源 | 备注 |
|---|---|---|---|---|
| `Characters/Player.tscn` | CharacterBody3D | PlayerCharacter.gd + 7 个组件 + ASC | SpriteFrames_Player.tres / Ability_PlayerBasicAttack.tres | M9 已 3D 化（Capsule + Sphere + Box）；DIR-2 后属性走 ConfigCenter `player_lv1`，不再直绑 .tres |
| `Characters/Enemy_Slime.tscn` | CharacterBody3D | EnemyCharacter.gd | Ability_EnemyBasicAttack.tres + 内嵌 Slime SpriteFrames | M6 数据驱动 / M9 3D 化 |
| `Characters/Boss_Demo.tscn` | CharacterBody3D | EnemyCharacter.gd（+ BossAI） | Ability_BossBasicAttack.tres | M5 / M9 3D 化（紫色 BoxMesh 占位） |

## 三、Scenes/Levels/（2 份）

| 文件 | 根节点 | 挂载脚本 | 关键内容 |
|---|---|---|---|
| `Levels/TestArena.tscn` | Node3D | `Scenes/Levels/test_arena.gd` | Player + 3 Slime + HUD + DirectionalLight3D + WorldEnvironment + Pickup ×2 + BossPortal（Item_HealthPotion / Item_IronSword / LevelDef_BossRoom_01） |
| `Levels/BossRoom_01.tscn` | Node3D | `Scenes/Levels/boss_room.gd` | Boss_Demo + 紫色光照 |

## 四、Scenes/UI/（6 份，HUD 新旧两套并存）

| 文件 | 根节点 | 挂载脚本 | 角色 | 状态 |
|---|---|---|---|---|
| `UI/HUD.tscn` | Control | HUD.gd | 玩家 HUD 主控（HP/MP/技能槽） | 🟢 现行 / HUD Phase 2 时拆分 |
| `UI/HUD_Main.tscn` | CanvasLayer | （场景文件，无 root 脚本） | 8 层 CanvasLayer + 7 Slot 框架 | 🟡 P1 已生成，未接入 |
| `UI/BossHealthBar.tscn` | Control | BossHealthBar.gd | Boss 主血条 | 🟢 现行 |
| `UI/InventoryUI.tscn` | Control | InventoryUI.gd | 背包 | 🟢 现行 / Phase 2 改 BaseWidget |
| `UI/PauseMenu.tscn` | Control | PauseMenu.gd | 暂停菜单 | 🟢 现行 / Phase 2 改 BaseWidget |
| `UI/SettingsMenu.tscn` | Control | SettingsMenu.gd | 设置菜单 | 🟢 现行 / Phase 2 改 BaseWidget |

> ⚠️ **HUD 新旧并存**：当前 TestArena 用 `HUD.tscn`（旧），`HUD_Main.tscn`（新）已生成但未替代旧版。HUD Phase 2 完成时 HUD.tscn 拆分为 PlayerInfoWidget + HotbarWidget 后落到 HUD_Main 内的 Slot 中。

## 五、addons/（3 份插件场景）

| 文件 | 用途 |
|---|---|
| `addons/skill_editor/dock/SkillEditorDock.tscn` | 底部 Dock 主面板（M7.4-M7.6） |
| `addons/skill_editor/dock/preview/PreviewScene.tscn` | 编辑器内嵌预览（M7.7） |
| `addons/godot_mcp/ui/mcp_panel_native.tscn` | MCP 插件原生面板 |

---

## 六、引用关系图（精简版）

```
project.godot.run/main_scene
    └── TestArena.tscn
            ├── Player.tscn ─→ ConfigCenter.player_lv1（DIR-2 完成 2026-05-21，已解耦）
            │                ─→ Ability_PlayerBasicAttack.tres ─→ Timeline_PlayerBasicAttack.tres
            │                ─→ SpriteFrames_Player.tres
            ├── Enemy_Slime.tscn (×3) ─→ Ability_EnemyBasicAttack.tres ─→ Timeline_EnemyBasicAttack.tres
            ├── HUD.tscn (旧) ─→ DamagePopupPool / OverheadHealthBarManager / ...
            ├── Pickup × 2 ─→ Item_HealthPotion.tres / Item_IronSword.tres
            └── BossPortal ─→ LevelDef_BossRoom_01.tres
                              └── BossRoom_01.tscn
                                    └── Boss_Demo.tscn ─→ Ability_BossBasicAttack.tres ─→ Timeline_BossBasicAttack.tres

addons/skill_editor/SkillEditorDock.tscn
    └── PreviewScene.tscn ─→ PreviewStageConfig.tres ─→ SpriteFrames_Player.tres
```

---

## 七、整理建议（汇入 C3）

| 文件 | 决策 |
|---|---|
| `Scenes/UI/HUD_Main.tscn` | 保留（HUD Phase 1 已落地） |
| `Scenes/UI/HUD.tscn` | 保留（HUD Phase 2 时拆分） |
| 其余 12 份 | 全部保留 |

无可删场景。

---

> **C0-T3 完成标记**：✅ 已生成。
