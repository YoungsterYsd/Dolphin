# 02 · 资源清单（C0-T2）

> **生成日期**：2026-05-21
> **统计**：`Data/**/*.tres` 共 **42 份**；引用关系通过 grep 全工程 `path="res://Data/...` 反推。
> **三期归属**：✅ FromExcel / 📜 Manual / 🗑 _Legacy 三档，对齐 `三期开发计划.md` 附录 A。

---

## 一、按目录分类（含引用台账）

### 1. `Data/Abilities/`（3 份，📜 Manual）

| 文件 | 顶级类 | 被引用方 | 三期归属 |
|---|---|---|---|
| `Ability_PlayerBasicAttack.tres` | Ability_TimelineDriven | Player.tscn | 📜 Manual（含逻辑分支） |
| `Ability_EnemyBasicAttack.tres` | Ability_TimelineDriven | Enemy_Slime.tscn | 📜 Manual |
| `Ability_BossBasicAttack.tres` | Ability_TimelineDriven | Boss_Demo.tscn | 📜 Manual |

### 2. `Data/Animations/`（2 份，📜 Manual）

| 文件 | 顶级类 | 被引用方 | 备注 |
|---|---|---|---|
| `SpriteFrames_Player.tres` | SpriteFrames | Player.tscn / PreviewStageConfig.tres / addons/skill_editor PreviewScene.tscn | 玩家动画 |
| `Slime.tres` | SpriteFrames | （内嵌于 Enemy_Slime.tscn，**0 外部引用**） | ⚠️ 实际被 .tscn 内嵌；这份单独 .tres 待人工待办确认是否还要保留 |

### 3. `Data/Attributes/`（3 份，🗑 待处理 ← 重要修订）

| 文件 | 顶级类 | 被引用方 | 处理方案 |
|---|---|---|---|
| `SlimeAttributes.tres` | CharacterAttributeSet | （仅 Plans/ 文档提及） | ✅ **可直接 git rm**（M6 后 Slime 走 ConfigCenter） |
| `BossAttributes.tres` | CharacterAttributeSet | （仅 Plans/ 文档提及） | ✅ **已 git rm（2026-05-21）** |
| `DefaultPlayerAttributes.tres` | CharacterAttributeSet | ~~Player.tscn line 11/82 + main_scene.tscn line 8~~ | ✅ **已 git rm（2026-05-21）**：DIR-2 完成后 Player 走 ConfigCenter `player_lv1`，TrainingDummy 改 inline SubResource，整目录 `Data/Attributes/` 已删 |

> ⚠️ **整理决策修订**：用户原拍板"3 个全删"。盘点后发现 PlayerAttributes 仍被两个 .tscn 引用，删除会导致 Player 加载失败。建议改为：
> - **C3 阶段**：先 git rm 2 份（Slime / Boss）
> - **第二步 开发方向**：把"PlayerAttributes 接入数据驱动"列入 P0（约 0.3 天，与 M6 EnemyCharacter 改造同模式）
> - **第三步 重构后**：再 git rm DefaultPlayerAttributes.tres

### 4. `Data/Config/`（19 份，分两组）

#### 4.1 顶级 Config（13 份）

| 文件 | 顶级类 | 被谁加载 | 三期归属 |
|---|---|---|---|
| `CameraConfig.tres` | CameraConfig | ConfigCenter | 📜 Manual（TA 调试） |
| `CharacterInstances.tres` | CharacterInstanceTable | ConfigCenter | ✅ FromExcel |
| `HealthBarConfig.tres` | HealthBarConfig | ConfigCenter | 📜 Manual（UI 微调） |
| `HitFeedbackConfig.tres` | HitFeedbackConfig | ConfigCenter | 📜 Manual（HDR 颜色） |
| `HUDLayout_Default.tres` | HUDLayoutResource | UIExtensionSubsystem / main_scene.gd（HUD Phase 1 待启用） | 📜 Manual |
| `LightingConfig.tres` | LightingConfig | ConfigCenter | 📜 Manual |
| `PostProcessConfig.tres` | PostProcessConfig | ConfigCenter | 📜 Manual |
| `PreviewStageConfig.tres` | PreviewStageConfig | addons/skill_editor | 📜 Manual（编辑器内部） |
| `SfxBindings.tres` | SfxBindings | AudioManager / ConfigCenter | ✅ FromExcel |
| `SkillDamageTable.tres` | SkillDamageTable | ConfigCenter | ✅ FromExcel |
| `UIDurations.tres` | UIDurations | ConfigCenter（HUD P0 加载，Phase 1 BaseWidget 接入） | 📜 Manual |

#### 4.2 子目录（6 份 InputContexts + 2 份 GrowthTables）

| 子目录 | 文件数 | 顶级类 | 三期归属 |
|---|---|---|---|
| `Config/InputContexts/` | 6 份（Gameplay / PanelOpen / Modal / Dialogue / Cutscene / Dead） | InputContext | 📜 Manual（HUD P0-T3） |
| `Config/AttributeGrowthTables/` | 2 份（Growth_Slime / Growth_Boss） | AttributeGrowthTable | ✅ FromExcel |

### 5. `Data/Effects/`（7 份，✅ FromExcel）

| 文件 | 顶级类 | 当前用途 |
|---|---|---|
| `GE_BasicDamage.tres` | GameplayEffect | 玩家普攻伤害 |
| `GE_EnemyMeleeDamage.tres` | GameplayEffect | Slime 普攻 |
| `GE_BossMeleeDamage.tres` | GameplayEffect | Boss 普攻 |
| `GE_Burning_3s.tres` | GameplayEffect | DoT 示例 |
| `GE_Cleanse.tres` | GameplayEffect | 净化 |
| `GE_Heal30.tres` | GameplayEffect | 药水回血 |
| `GE_Stun_2s.tres` | GameplayEffect | 眩晕 |

> ⚠️ 注：根据 grep 结果，`Data/Effects/*.tres` 当前**无场景/资源直接引用**。这些 GE 都是通过 `Ability_*.tres` / `SkillDamageTable.tres` / `Item_HealthPotion.tres` 间接装配，是合规的；不属于死资源。

### 6. `Data/Items/`（2 份，✅ FromExcel）

| 文件 | 顶级类 | 被引用方 |
|---|---|---|
| `Item_HealthPotion.tres` | ConsumableDefinition | TestArena.tscn（Pickup 节点） |
| `Item_IronSword.tres` | EquipmentDefinition | TestArena.tscn（Pickup 节点） |

### 7. `Data/Levels/`（1 份）

| 文件 | 顶级类 | 被引用方 | 三期归属 |
|---|---|---|---|
| `LevelDef_BossRoom_01.tres` | LevelDef | TestArena.tscn 的 BossPortal | 部分 📜 Manual（含 PackedScene 引用） |

### 8. `Data/Skills/Timelines/`（4 份，📜 Manual · 由 Skill Editor 维护）

| 文件 | 顶级类 | 被加载方 |
|---|---|---|
| `Timeline_PlayerBasicAttack.tres` | SkillTimeline | ConfigCenter（按 skill_id 索引） |
| `Timeline_EnemyBasicAttack.tres` | SkillTimeline | ConfigCenter |
| `Timeline_BossBasicAttack.tres` | SkillTimeline | ConfigCenter |
| `Timeline_Test.tres` | SkillTimeline | 单测脚本 + Skill Editor 预览 |

### 9. `Data/Tags/`（1 份，📜 Manual）

| 文件 | 顶级类 | 被加载方 |
|---|---|---|
| `GameplayTags.tres` | GameplayTagRegistry | GameInstance / ConfigCenter |

### 10. `Data/Enemies/`（**空目录** ← C3-T3 处理）

`Data/Enemies/` 当前 0 文件；三期文档明确该目录已废弃。

---

## 二、空目录与孤儿（→ C3-T3）

| 路径 | 状态 | 处理 |
|---|---|---|
| `Data/Enemies/` | 空目录 | C3-T3：删除 |

---

## 三、整理建议清单（汇入 C3）

| 操作 | 文件 | 决策 |
|---|---|---|
| `git rm` | `Data/Attributes/SlimeAttributes.tres` | ✅ 用户已拍板 + 0 引用确认 |
| `git rm` | `Data/Attributes/BossAttributes.tres` | ✅ **已完成 2026-05-21**（DIR-2 一并清理） |
| ~~延后~~ ✅ | `Data/Attributes/DefaultPlayerAttributes.tres` | ✅ **已完成 2026-05-21**（DIR-2 完成后整目录已 git rm） |
| `rmdir` | `Data/Enemies/` | C3-T3 |
| 复核 | `Data/Animations/Slime.tres` | C3-T3：grep 0 引用，但 Slime.tscn 内嵌 SpriteFrames，确认是否删除独立 .tres |

---

## 四、统计摘要

| 维度 | 数量 |
|---|---|
| ✅ FromExcel（三期 M10 可走 Excel） | 14（Characters 1 / Growth 2 / Effects 7 / Items 2 / SfxBindings 1 / SkillDamageTable 1） |
| 📜 Manual（程序员 / TA 维护） | 25（13 顶级 Config + 6 InputContexts + 1 Tags + 4 Timelines + 1 LevelDef） |
| 🗑 待删除 | 3（2 已确认 + 1 待 Player 改造后） |
| **总计** | **42** |

---

> **C0-T2 完成标记**：✅ 已生成。
