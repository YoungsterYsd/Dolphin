# 06 · 待整理项（C0-T6）

> **生成日期**：2026-05-21
> **数据来源**：grep 全工程 `TODO`/`FIXME`/`HACK`/`XXX`/`DEPRECATED` + 0 引用扫描 + 临时脚本识别。
> **核心结论**：项目代码层非常干净（Script/ 中 0 条 TODO/FIXME），主要欠账集中在 **3 个临时脚本** + **3 张旧资源** + **1 个孤儿 uid** + **1 个空目录**。

---

## 一、临时脚本与归档（→ C3-T1）

| 路径 | 大小 | 性质 | 处理 |
|---|---|---|---|
| `Tools/_phase1_verify.gd` | ~2-3KB | HUD Phase 0 验收用临时脚本 | C3-T1：移至 `Tools/_Archive/Phase1Verify/` |
| `Tools/_phase1_verify.gd.uid` | - | 同上 | 同上 |
| `Tools/_phase1_verify.tscn` | - | 同上 | 同上 |
| `Tools/_p1_log.txt` | - | 验收日志 | 同上 |

---

## 二、0 引用脚本（→ C3-T4）

**结论**：`Script/` 96 个 .gd 全部至少被 `extends`/`preload`/`load` 一次引用。**0 死代码脚本**。

唯一例外：`Tools/_phase1_verify.gd` 是顶层临时脚本，已在 C3-T1 处理。

---

## 三、0 引用 .tres（→ C3-T2 / C3-T3）

| 路径 | 状态 | 处理 |
|---|---|---|
| `Data/Attributes/SlimeAttributes.tres` | 0 工程引用（仅 Plans/ 提及） | ✅ **C3-T2：git rm**（用户已拍板） |
| `Data/Attributes/BossAttributes.tres` | 0 工程引用 | ✅ **C3-T2：git rm（已完成 2026-05-21）** |
| `Data/Attributes/DefaultPlayerAttributes.tres` | ❌ 仍被 Player.tscn / main_scene.tscn 引用 | ✅ **已完成 2026-05-21**：DIR-2 已结清，三份 .tres + 空目录已 git rm |
| `Data/Animations/Slime.tres` | 0 外部引用（Slime.tscn 内嵌 SpriteFrames） | C3-T3：grep 二次确认后 git rm |

---

## 四、空目录（→ C3-T3）

| 路径 | 处理 |
|---|---|
| `Data/Enemies/` | C3-T3：rmdir |

---

## 五、孤儿 .uid（→ C3-T3）

| 路径 | 状态 | 处理 |
|---|---|---|
| `Script/Character/Components/ShadowDecal.gd.uid` | .gd 已删除，仅留 .uid | C3-T3：git rm |

---

## 六、临时文档锁文件（→ C1-T4）

| 路径 | 来源 |
|---|---|
| `Plans/Dolphin设计/~$_HUD组件化设计_交互与落地.docx` | Word/WPS 锁文件 |
| `Plans/Dolphin设计/~$HUD策划案.docx` | 同上 |
| `Plans/Dolphin设计/~$HUD策划案.xlsx` | 同上 |

> **处理**：C1-T4 全部 git rm（Word 临时文件，不应入库）。

---

## 七、@tool 但有"workaround 注释"的脚本（→ 第三步保留参考）

来自 `二期开发计划.md` 第 578 条变更记录：

> 工程踩坑记录：(1) 所有 SkillSystem Resource/Track/Keyframe 类必须加 @tool 标注；(2) plugin.gd 不要用 preload；(3) 编辑器内通过 SkillTimeline.get_track_keyframes 间接访问轨道关键帧。

涉及 12 个 @tool 脚本（详见 `01_代码清单.md` 第 8 节）。**保留现状**；第三步可考虑封装到工具方法消除 workaround 注释。

---

## 八、注释里的 TODO/FIXME（grep 结果）

**Script/ 全工程 0 条**。

`Plans/Dolphin设计/` 内 23 条命中均为文档章节名（"Todo 清单"等），非真实欠账。

---

## 九、HUD 新旧两套并存（→ HUD Phase 2 而非 C 阶段）

| 旧 | 新 | 处理 |
|---|---|---|
| `Script/UI/HUD.gd`（玩家 HUD 主控） | 拆为 PlayerInfoWidget + HotbarWidget | HUD Phase 2 |
| `Scenes/UI/HUD.tscn` | `Scenes/UI/HUD_Main.tscn` | HUD Phase 2 |
| 旧 widget 9 份（直 cast 业务类） | 新 BaseWidget + Contracts 接口 | HUD Phase 2 |

**不在 C 阶段处理**；只在 `Plans/技术债清单.md`（C4-T2）中作为 P0 列出。

---

## 十、操作汇总（C3 总清单）

| 操作 | 路径 | 来源 todo |
|---|---|---|
| `git mv` | `Tools/_phase1_verify*` + `_p1_log.txt` → `Tools/_Archive/Phase1Verify/` | C3-T1 |
| `git rm` | `Data/Attributes/SlimeAttributes.tres` | C3-T2 |
| `git rm` | `Data/Attributes/BossAttributes.tres` | C3-T2 |
| `git rm` | `Data/Animations/Slime.tres`（grep 二次确认后） | C3-T3 |
| `rmdir` | `Data/Enemies/` | C3-T3 |
| `git rm` | `Script/Character/Components/ShadowDecal.gd.uid` | C3-T3 |
| `git rm` | `Plans/Dolphin设计/~$*.docx` / `~$*.xlsx` | C1-T4 |
| `git rm -r` | `Plans/前项目/` | C1-T3（用户已拍板） |

> 共 **8 类操作**，约 **14 个文件 + 1 个目录** 物理变更；**0 行业务代码改动**。

---

## 十一、第二步开发方向输入

由本盘点反推的开发欠账（汇入第二步）：

| ID | 欠账 | 影响 | 优先级 | 状态 |
|---|---|---|---|---|
| ~~**D1**~~ | ~~Player 接入 ConfigCenter（消除 DefaultPlayerAttributes.tres 直引）~~ | ~~数据驱动一致性~~ | ~~P0~~ | ✅ **已完成 2026-05-21**（DIR-2，详见 07_错误清单 §十一） |
| **D2** | HUD Phase 1 末态接入（HUDManager.setup() 等） + Phase 2 现状迁移 | HUD 双套并存 | P0 | ⬜ 未开始 |
| **D3** | M9.7 DOF 接入（CameraAttributesPractical） | 视觉欠账 | P2 |
| **D4** | 技能编辑器 Undo/Redo（M7.5 留作后续优化） | 编辑器体验 | P2 |
| **D5** | ProjectileSpawner（M8 计划但未实装） | 远程武器/法术 | P1 |
| **D6** | M10 Excel 工作流（三期主线） | 数据驱动彻底化 | P0 |
| **D7** | M11 Dialogue + M12 NPC（三期主线） | 内容扩展 | P1 |
| **D8** | CombatStateService（EventBus.combat_state_changed 仅声明） | 战斗节奏 | P2 |
| **D9** | SfxBindings 实际音频资源（人工待办 M8.2） | 音效缺位 | P1 |
| **D10** | M9.8 美术资源（Slime SpriteFrames 抽出 / Boss 占位 Mesh / 地面贴图） | 视觉占位 | P2 |

---

> **C0-T6 完成标记**：✅ 已生成。
