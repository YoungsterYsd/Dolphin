# 规则检查报告 2026-05-19（M6 里程碑）

- 触发时机：**T1（M6 里程碑完成验收前）+ T2（新增 Autoload `ConfigCenter` + 5 个 Resource 类 + 1 个静态工具类）**
- 扫描范围：**全量 11 条规则 + 重点关注 M6 变更点**
- 总体结论：✅ **通过**。**0 项 Error，0 项 Warning**。建议**放行 M6 验收**。

---

## 本轮重点关注项核对

| 关注点 | 结论 | 证据 |
|---|---|---|
| R-ARCH-02 同步（project.godot ↔ 全局规则白名单） | ✅ 完全一致（6/6） | 见下方 R-ARCH-02 |
| R-DIR-01 新增 `Script/Data/` 合规 | ✅ 目录与扩展名匹配 | 见下方 R-DIR-01 |
| R-NAME-01 新脚本命名合规 | ✅ 7 个新 .gd 全部声明 `class_name` 且文件名 PascalCase 一致 | 见下方 R-NAME-01 |
| R-DATA-01 / R-DATA-02 新代码无硬编码玩家数值 | ✅ 数值已外置到 `Data/Config/*.tres`；新代码内无魔数 | 见下方 R-DATA-02 |
| R-LOG-01 新代码统一 GameLogger | ✅ ConfigCenter / EnemyCharacter / AttributeResolver 全部走 GameLogger | 见下方 R-LOG-01 |
| R-EVENT-01 / R-GAS-01 ~ R-GAS-03 未改动兼容 | ✅ 未发现回归 | 见下方各规则 |

---

## 各规则详细结论

### R-ARCH-01 · 跨模块通信走 EventBus —— ✅ 通过
- 全工作区 grep `get_node\("/root/` 与 `get_parent\(\)\.get_parent` 均 0 命中。
- `EnemyCharacter._inject_data_driven_attributes()` 中 `get_tree().root.get_node_or_null(^"ConfigCenter")` 是「业务节点访问 Autoload」，不属于「跨模块直接 grab 别的业务节点的子组件」语义，且 ConfigCenter 是规则明文白名单内的全局服务节点，**合规且符合 ConfigCenter.gd 第 7-8 行的设计意图说明**（规避静态识别 Autoload 失败的兼容写法）。
- `_find_player()` 通过 `get_tree().get_nodes_in_group(&"player")` 做索敌，属于松耦合查询，合规。

### R-ARCH-02 · Autoload 数量上限 6 —— ✅ 通过
- `project.godot [autoload]` 实际 6 项：
  1. `ConfigCenter="*res://Script/Core/ConfigCenter.gd"` ✅ 新增（M6）
  2. `EventBus="*res://Script/Core/EventBus.gd"`
  3. `GameInstance="*res://Script/Core/GameInstance.gd"`
  4. `LevelManager="*res://Script/GameFramework/LevelManager.gd"`
  5. `AudioManager="*res://Script/Audio/AudioManager.gd"`
  6. `SettingsManager="*res://Script/Settings/SettingsManager.gd"`
- 全局规则 R-ARCH-02 白名单：`EventBus / GameInstance / LevelManager / AudioManager / SettingsManager / ConfigCenter`（M6 起新增）
- **6/6 完全一致，零差异**。规则变更记录（2026-05-19）也已对应。

### R-GAS-01 · GameplayTag 必须注册 —— ✅ 通过（M6 未引入新 tag）
- M6 新代码（ConfigCenter / 5 个 Resource / AttributeResolver / EnemyCharacter 改造）未引入新 GameplayTag 字面量。
- `Data/Tags/GameplayTags.tres` 注册表无变化（24 个 tag）。
- 本轮 T2 不涉及 tag 检查；M3-M5 既有 tag 一致性沿用前次报告结论。

### R-GAS-02 · 属性必须经 set_attr 修改 —— ✅ 通过
- grep `attribute_set\.\w+\s*=` 命中 3 处，逐一核对：
  1. `Script/Character/EnemyCharacter.gd:99` —— `asc_node.attribute_set.owner_node = self`：给 **AttributeSet 自身的回指字段**赋值，**不是给"属性"赋值**，不属于 R-GAS-02 范围。
  2. `Script/GAS/AttributeSet.gd:4` —— 注释行说明本规则。
  3. `Script/GAS/AbilitySystemComponent.gd:43` —— `attribute_set.owner_node = get_parent()`：同 #1，给 owner_node 引用赋值，非属性赋值。
- M6 关键管线 `AttributeResolver.apply_to_attribute_set()` 第 74/80 行通过 `attr_set.set_attr(k, ...)` 写入，**完全走标准接口**，触发 clamp + 信号广播。✅

### R-GAS-03 · Ability/GE 必须 Resource 化 —— ✅ 通过（M6 未改动 Ability/GE）
- M6 未新增/改动 `Script/GAS/Abilities/` 与 `Script/GAS/Effects/`。
- 既有 `Data/Abilities/Ability_EnemyBasicAttack.tres` / `Ability_BossBasicAttack.tres` 在 Slime/Boss 场景中仍以 ExtResource 注入 `startup_ability_set`，合规。

### R-DIR-01 · 目录分层 —— ✅ 通过
- `Script/` 下 0 个 `.tscn` 文件 ✅
- `Scenes/` 下仅 3 个 `.gd`，且全部与同目录 `.tscn` 同名（`main_scene.gd` / `boss_room.gd` / `test_arena.gd`），属于 R-DIR-01 明文允许的「场景内部同名脚本」例外 ✅
- `Data/` 下 0 个 `.gd`、0 个 `.tscn`，仅 `.tres` 与 `README.md` ✅
- 新增 `Script/Data/` 目录纯放 Resource 定义脚本（无场景、无 .tres），目录用途清晰 ✅

### R-NAME-01 · 命名规范 —— ✅ 通过
M6 新增脚本逐项核对（声明 `class_name` → 文件名应为 `<ClassName>.gd`）：

| 文件 | class_name | 是否一致 |
|---|---|---|
| `Script/Core/ConfigCenter.gd` | （Autoload，无 class_name）| ✅ 匹配 Autoload 注册名 `ConfigCenter` |
| `Script/Data/GrowthSegment.gd` | `GrowthSegment` | ✅ |
| `Script/Data/AttributeGrowthEntry.gd` | `AttributeGrowthEntry` | ✅ |
| `Script/Data/AttributeGrowthTable.gd` | `AttributeGrowthTable` | ✅ |
| `Script/Data/CharacterInstanceEntry.gd` | `CharacterInstanceEntry` | ✅ |
| `Script/Data/CharacterInstanceTable.gd` | `CharacterInstanceTable` | ✅ |
| `Script/Data/AttributeResolver.gd` | `AttributeResolver` | ✅ |

GameplayTag 命名：M6 未新增 tag，沿用既有 `lower.dot.case` 风格 ✅
信号命名：M6 未新增 signal ✅
配置文件命名：`CharacterInstances.tres` / `Growth_Slime.tres` / `Growth_Boss.tres` 风格统一，建议保持 ✅

### R-CHAR-01 · 2D/3D 通用 API —— ✅ 通过
- M6 未引入新 Component 类；`EnemyCharacter` 新增的 `_inject_data_driven_attributes()` 为内部 private 方法（前缀 `_`），不属于 public API 范围。
- 新增 `@export var entity_id: StringName` / `@export var level_override: int` —— 标量类型，与 2D/3D 无关，合规。

### R-DATA-01 · 玩家可见数值数据化 —— ✅ 通过
- M6 的核心目标即是把 HP / 攻击 / 防御 / 移速等玩家可见数值**从 AttributeSet .tres 迁移到成长表**。
- 实际数值落在 `Data/Config/AttributeGrowthTables/Growth_Slime.tres`（HP 30→各段 +5/+8，atk 5→+1/级，def 2→+0.5/级，spd 80）与 `Growth_Boss.tres`（HP 200→+50/级，atk 12→+2/级，def 4→+1/级，spd 100）。**全部 Resource 化**。
- 角色实例数据落在 `Data/Config/CharacterInstances.tres`（3 个条目 slime_lv1 / slime_elite_lv5 / boss_demo_lv1）。
- 场景 `.tscn` 仅留 `entity_id = &"slime_lv1"` / `&"boss_demo_lv1"` 这种**id 引用**，无数值字面量泄漏。
- 注：场景 `MoveComponent.max_speed = 80.0/100.0` 在 `_inject_data_driven_attributes()` 第 110-111 行运行时会被覆盖为成长表/override 值，属于"场景默认值会被运行时覆盖"模式，无害。

### R-DATA-02 · 数据驱动优先 —— ✅ 通过（重点规则，M6 起生效）
对 ConfigCenter / AttributeResolver / EnemyCharacter / 5 个 Resource 类做了数字字面量重点扫描（grep `[^\w\.]\d{2,}\.?\d*`），命中均为：
- 注释中的示例数字（如 `99999` / `5/+5` / `10 级`）
- Resource 类的 `@export` **接口默认值**（如 `breakpoint_level: int = 5`、`per_level_delta: float = 0.0`、`level: int = 1`、`move_speed_override: float = -1.0`、`base_value: float = 0.0`）

**无任何"逻辑流程中的硬编码数值"**。所有可调玩法值都通过 `ConfigCenter.get_character_def()` / `get_attribute_growth_table()` / `resolve_character_attributes()` 走 `Data/Config/*.tres`。

ConfigCenter 自身的常量路径（`CHARACTER_INSTANCES_PATH` / `GROWTH_TABLES_DIR`）以 `const` 集中声明在文件头部并加注释「R-DATA-02：路径也走常量集中管理」，符合规则精神。✅

EnemyCharacter 的可见数字字面量：仅注释。`level_override: int = -1` / `entity_id: StringName = &""` 均为接口默认值。✅

### R-EVENT-01 · 全局信号集中声明 —— ✅ 通过
- 全工作区 `signal` 共 24 条：
  - **EventBus 内 13 条**：均为全局意图信号（attribute_changed / ability_* / effect_* / damage_dealt / player_died / enemy_died / level_* / boss_phase_changed / inventory_* / hud_toast_requested 等）✅
  - **业务节点 11 条**：全部用于自身/父子作用域（InputComponent 3 条、HurtboxComponent.damaged、HitboxComponent.hit_landed、AnimationComponent.animation_finished、GameplayTagContainer 2 条、AIController.state_changed），命名作用域语义清晰，不属"全局意图"。✅
- M6 新增的 6 个 Resource 类与 ConfigCenter / AttributeResolver 均**未声明 signal**。✅
- `EnemyCharacter._on_attr_changed` 通过 `EventBus.attribute_changed.connect(...)` 订阅 EventBus，是规范用法。✅

### R-LOG-01 · GameLogger 带 channel —— ✅ 通过
- 全工作区 grep `\bprint|print_rich|push_warning|push_error\(` 命中均在 `Script/Core/GameLogger.gd` 内部实现（第 20/24/28 行），属于规则明文豁免。
- M6 新代码使用 channel 核对：
  - `ConfigCenter.gd`：8 处 `GameLogger.info/warn/error("Config", ...)` ✅
  - `EnemyCharacter.gd`：新增 3 处 `GameLogger.info/warn("Character", ...)` ✅
  - `AttributeResolver.gd`：静态工具类，无日志（合理）
  - 5 个 Resource 类：纯数据类，无日志（合理）
- channel 命名一致，与既有 `"GAS"/"AI"/"UI"/"Level"/"Audio"/"Settings"/"Character"/"Core"` 兼容；M6 引入 `"Config"` 是新增 channel，规则未限制 channel 枚举（规则原文：channel 取值"如"），**合规**。

---

## 通过的规则汇总

- R-ARCH-01 ✅
- R-ARCH-02 ✅（6/6 白名单一致）
- R-GAS-01 ✅（M6 未引入新 tag）
- R-GAS-02 ✅
- R-GAS-03 ✅（M6 未改 Ability/GE）
- R-DIR-01 ✅
- R-NAME-01 ✅（7 个新文件全部命名合规）
- R-CHAR-01 ✅
- R-DATA-01 ✅
- R-DATA-02 ✅（**M6 起生效，本轮首次扫描，零违规**）
- R-EVENT-01 ✅
- R-LOG-01 ✅

---

## 给主 agent 的建议

1. **M6 验收建议：放行**。所有 Error 级规则零违规，所有 Warning 级规则零违规，达到 M6 验收标准。建议主 agent 在 `Plans/开发计划.md` / `Plans/二期开发计划.md` 标记 M6 完成。

2. **R-DATA-02 首次实操效果良好**：本次新增的 ConfigCenter + 数据驱动管线是 R-DATA-02 落地的标杆样本，建议沿用本套模式继续扩展：
   - M7 接入「技能数据」时建议复用 `Script/Data/` 目录 + `Data/Config/SkillData/` 子目录 + `ConfigCenter.get_skill_def(id)` API（ConfigCenter.gd 第 19-22 行已预留注释）
   - M8/M9 接入「HUD / HitFeedback / Camera」配置同理（同样已预留注释）

3. **建议提一份脚手架文档**：把 M6 的「`@export var entity_id` + `_inject_data_driven_attributes()` + `ConfigCenter.resolve_*()`」模式总结为《数据驱动接入模板.md》放在 `Plans/` 下，便于后续 M7-M10 复用，也便于评审 R-DATA-02 是否被遵循。（非阻塞，建议主 agent 与用户确认是否需要）

4. **次要观察（非违规，仅记录）**：
   - `EnemyCharacter._inject_data_driven_attributes()` 第 79 行用 `get_tree().root.get_node_or_null(^"ConfigCenter")` 而非直接写 `ConfigCenter.xxx`，已在脚本第 78 行注释解释（避免静态识别失败）。这是合理的防御性写法，但**当 ConfigCenter 排在 [autoload] 第 1 位、且 EnemyCharacter._ready 内执行时**，Autoload 一定已就绪，直接 `ConfigCenter.get_character_def(...)` 也是安全的。建议主 agent 与用户确认是否在后续里程碑切换为更简洁的写法（非合规问题，仅风格建议）。
   - `Scenes/Main/main_scene.tscn` 与 `Scenes/Characters/Player.tscn` 仍有 `attribute_set = ExtResource(...)` 直绑（即玩家角色尚未迁移到数据驱动）。**这不在 M6 改造范围**，预期可能在 M7+ 处理；如后续要扩展，可同样新增 `player_lv1` 条目。当前不算违规。

---

**报告结论：M6 里程碑零违规，建议直接放行验收。**
