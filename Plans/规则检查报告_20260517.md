# 规则检查报告 2026-05-17

- 触发时机：T1（M1 里程碑完成，验收前）
- 扫描范围：全量（11 条规则）
- 总体结论：✅ 通过 — 0 项 Error，2 项 Warning（均不阻塞验收）

## Error 级违规

无。

## Warning 级违规

### [R-DIR-01 衍生 / R-NAME-01] 场景内部脚本与场景文件不同名
- 文件：`Scenes/Main/m1_test_driver.gd`（与同目录 `main_scene.tscn` 共存）
- 命中内容：场景文件名 `main_scene.tscn`，挂载脚本名 `m1_test_driver.gd`，二者不一致。R-DIR-01 原文要求 "Scenes/ 下不应出现 .gd，除非该 .gd 是该场景内部脚本且文件名与场景一致"。
- 严重级评估：脚本顶部注释明确声明 "M3 起将由 PlayerCharacter 接管该逻辑，本脚本届时删除"，是临时驱动脚本，非长期资产。
- 整改建议（任选其一）：
  1. 维持现状但在 M3 验收前必须删除（推荐，已有注释承诺）；
  2. 立即重命名为 `main_scene.gd`，并同步修改 `main_scene.tscn` 中的脚本引用；
  3. 将 `m1_test_driver.gd` 移至 `Script/Test/` 之类的专用目录。
- 不阻塞 M1 验收，但建议在 `Plans/开发计划.md` M3 章节里把"删除 m1_test_driver.gd"列为 must-do。

### [R-NAME-01] 信号命名缺过去式语态
- 文件：`Script/Character/Components/HitboxComponent.gd:9`
- 命中内容：`signal hit(target: Node)`
- 问题：R-NAME-01 要求信号为 snake_case 过去式（参考 EventBus 中 `enemy_died` / `attribute_changed` 等示例）。`hit` 在英语里动词原形与过去式同形，语义模糊。
- 整改建议：重命名为 `hit_landed`。当前无订阅方，零迁移成本。
- 不阻塞 M1 验收。

## 通过的规则

- **R-ARCH-01 ✅**：grep `get_node("/root/` 与 `get_parent().get_parent` 在 `Script/` 全树 0 命中。`SettingsManager.set_*_volume` 调用 `AudioManager.set_bus_volume` 是合法的 Autoload→Autoload 调用。
- **R-ARCH-02 ✅**：`project.godot [autoload]` 段恰好 5 项 — `EventBus / GameInstance / LevelManager / AudioManager / SettingsManager`，与白名单完全一致。
- **R-CHAR-01 ✅**：`Script/Character/Components/` 下所有公开 API 接口签名均使用 `Vector3` 抽象。`MoveComponent` 内部 `Vector2` 投影属于"2D 实现内部"豁免；`AnimationComponent` 平台特化属于显式分流子类豁免。
- **R-DIR-01 ✅（主体通过）**：`Script/` 下 0 个 `.tscn`；`Data/` 全为子目录占位 + `README.md`；`Content/` 全为美术资产。仅 `m1_test_driver.gd` 同名问题降级为 Warning。
- **R-DATA-01 ✅**：业务代码无玩家可见数值魔数。`MoveComponent` 默认值（200/1600）属 `@export` 接口默认值；`SettingsManager` 0/1 是音量边界；`InputComponent.ability_pressed.emit(1/2)` 是槽位枚举值。M2 起须重点复扫。
- **R-EVENT-01 ✅**：业务节点 signal 全部为节点内/父子作用域（`InputComponent` / `HitboxComponent` / `HurtboxComponent` / `AnimationComponent`）；全局意图信号（`player_died` / `level_changed` 等）已正确集中在 `EventBus.gd`。
- **R-LOG-01 ✅**：业务代码 0 处裸 `print(`。`Logger.gd:30` 的 `print(line)` 是 Logger 内部实现，规则原文豁免。

## 不适用的规则

- **R-GAS-01**：M2 才引入 GameplayTag 系统，跳过。
- **R-GAS-02**：M2 才引入 `AttributeSet` 派生类，`Script/GAS/` 目录尚未创建，跳过。
- **R-GAS-03**：M2 才引入 Ability / GameplayEffect，跳过。

## 给主 agent 的建议

1. **是否阻塞 M1 验收：否**。0 项 Error，2 项 Warning 均为可延后处理项，建议放行 M1 验收。
2. **高频违规无需立刻写模板**。本次命中均为可解释边界情形，建议待 M2/M3 出现复发性命名违规后再写组件/信号样板。
3. **建议同步动作**（不阻塞）：
   - 在 `Plans/开发计划.md` M3 章节"PlayerCharacter 接管"任务后追加 must-do："删除 `Scenes/Main/m1_test_driver.gd`"；
   - 把 `HitboxComponent.signal hit` 改名为 `hit_landed`（零迁移成本，建议立即处理）。
4. **M2 启动后须重点复扫的规则**：R-GAS-01 / R-GAS-02 / R-GAS-03 / R-DATA-01 / R-EVENT-01。届时由 T2 触发点驱动再跑一次。
